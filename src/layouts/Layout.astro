---
import { ClientRouter } from 'astro:transitions';
import Sidebar from '../components/Sidebar.astro';
import BottomNav from '../components/BottomNav.astro';
import { API_URL } from '../config';

interface Props {
  title: string;
  description?: string;
}

const { title, description = 'Kulty Shop - Cuadros y arte para tu hogar' } = Astro.props;

let store = { logo: '', phone: '', instagram: '', facebook: '', tiktok: '' };
try {
  const res = await fetch(`${API_URL}/api/store`);
  if (res.ok) {
    store = await res.json();
  }
} catch (e) {}
---

<!doctype html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content={description} />
    <meta name="keywords" content="cuadros decorativos, arte para el hogar, lienzos, cuadros personalizados, decoración Colombia, arte en lienzo" />
    <link rel="icon" type="image/svg+xml" href={store.logo || "/favicon.svg"} />
    <title>{title}</title>
    <ClientRouter />
  </head>
  <body class="bg-gray-50 antialiased">
    <Sidebar store={store} />
    <BottomNav phone={store.phone || ''} />
    <div class="md:ml-20 pb-16 md:pb-0">
      <slot />
    </div>
  </body>
</html>

<style is:global>
  @import "../styles/global.css";
</style>
